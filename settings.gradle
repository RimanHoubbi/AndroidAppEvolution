pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //Chart library
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MyFitnessTracker"
include ':app'
